name: Tests
on:
  pull_request:
  schedule:
    - cron: '23 1 * * *'

jobs:
  pytest_minimal_requirements:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install toml
          python3 install_lowest_requirements.py
      - name: Test with pytest
        run: |
          pip install pytest
          pytest tests/
      - name: Test examples
        run: |
          pip install nbmake
          pytest --nbmake --nbmake-timeout 600 examples/
  pytest_qiskit_0_46:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[examples] qiskit==0.46.3 qiskit-ibm-runtime==0.20.0
      - name: Test with pytest
        run: |
          pip install pytest
          pytest tests/
      - name: Test examples
        run: |
          pip install nbmake
          pytest --nbmake --nbmake-timeout 600 examples/
  pytest_qiskit_1:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[examples] qiskit<1.2 qiskit-ibm-runtime<0.28
      - name: Test with pytest
        run: |
          pip install pytest
          pytest tests/
      - name: Test examples
        run: |
          pip install nbmake
          pytest --nbmake --nbmake-timeout 600 examples/

  pytest_latest: 
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: |
            3.9
            3.12
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install .[examples]
      - name: Test with pytest
        run: |
          pip install pytest
          pytest tests/
      - name: Test examples
        run: |
          pip install nbmake
          pytest --nbmake --nbmake-timeout 600 examples/
